# Django Configuration
SECRET_KEY=your-secret-key-here-generate-a-strong-random-key
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,.onrender.com

# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host.neon.tech/database?sslmode=require

# Google Gemini AI API (Optional - for AI chat features)
GEMINI_API_KEY=your-gemini-api-key-here

# Google Drive API (Optional - if using Drive integration)
# Place credentials.json file in project root (excluded by .gitignore)
